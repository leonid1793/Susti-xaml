<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lab14.TodoItemPage">
    <ContentPage.Content>
        <StackLayout Margin="20" VerticalOptions="StartAndExpand">
            <Label Text="Nombre" />
            <Entry Text="{Binding Nombre}" />
            <Label Text="Apellido" />
            <Entry Text="{Binding Apellido}" />
            <Label Text="DNI" />
            <Entry Text="{Binding DNI}" />
            <Label Text="Fecha Nac." />
            <DatePicker Date="{Binding FechaNa}" />
            <Label Text="Done" />
            <Switch IsToggled="{Binding Done}" />
            <Button Text="Guardar" Clicked="OnSaveClicked" />
            <Button Text="Borrar" Clicked="OnDeleteClicked" />
            <Button Text="Cancelar" Clicked="OnCancelClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
